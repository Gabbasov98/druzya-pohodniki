let burger = document.querySelector(".burger")
let body = document.querySelector("body")
let header = document.querySelector(".header")
let menuClose = document.querySelector(".m-menu__close")


if (burger) {
    burger.onclick = function() {
        header.classList.toggle("_open")
        body.classList.toggle("fixed-body")
    }
}

if(menuClose){
    menuClose.onclick = function () {
        closeMenu()
    }
}

function closeMenu(){
    header.classList.remove("_open")
    body.classList.remove("fixed-body")
}


let favouriteButtons = document.querySelectorAll(".favourite-btn")

favouriteButtons.forEach(btn => {
    btn.onclick = function(){
        this.classList.toggle("_active")
    }
})

function Sliders() {
    let SlidersArray = []

    let sliders = document.querySelectorAll(".slider")
    sliders.forEach((slider,index) => {
        slider.setAttribute("data-slider-id",index)

        SlidersArray[index] = new Swiper(`.slider[data-slider-id="${index}"] .swiper-container`, {
            slidesPerView: "auto",
            spaceBetween: 0,
            watchSlidesProgress: true,
            preventClicks :true,
            a11y: false,
            observer: true,
            observeParents: true,
            observeSlideChildren: true,
            navigation: {
                nextEl: `.slider[data-slider-id="${index}"] .swiper-button-next`,
                prevEl: `.slider[data-slider-id="${index}"] .swiper-button-prev`,
            },
            pagination: {
                el: `.slider[data-slider-id="${index}"] .swiper-pagination`,
                type: 'bullets',
                clickable: true,
            },
        })
    })
}

Sliders()

let tourNav = document.querySelector(".tour-nav")

setFixedTourNav()
window.onscroll = function (e) {
    setFixedTourNav()
};


function setFixedTourNav() {
    if(!tourNav){
        return
    }
    let tourNavHeight = tourNav.getBoundingClientRect().height
    let st = window.scrollY;
    if(st > tourNavHeight * 2){
        tourNav.classList.add("_fixed")
    } else{
        tourNav.classList.remove("_fixed")
    }
}


let tourProps = document.querySelectorAll(".tour-props")
tourProps.forEach(tourPropsItem => {
    let toggle = tourPropsItem.querySelector(".tour-props__btn")
    toggle.onclick = function () {
        tourPropsItem.classList.add("_show-all")
    }
})


let tourDesc = document.querySelectorAll(".tour-desc")
tourDesc.forEach(tourDescItem => {
    let toggle = tourDescItem.querySelector(".tour-desc__btn")
    toggle.onclick = function () {
        tourDescItem.classList.add("_show-all")
    }
})

window.addEventListener('DOMContentLoaded', () => {
    fix100vh();
    window.addEventListener('resize', () => {
        fix100vh();
        // setFixedHeader()
        // getScrollBarSize()
    })

    // setFixedHeader()
    // getScrollBarSize()


    new AirDatepicker('.date-input', {
        locale: {
            days: ['Воскресенье', 'Понедельник', 'Вторник', 'Среда', 'Четверг', 'Пятница', 'Суббота'],
            daysShort: ['Вос', 'Пон', 'Вто', 'Сре', 'Чет', 'Пят', 'Суб'],
            daysMin: ['Вс', 'Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб'],
            months: ['Январь', 'Февраль', 'Март', 'Апрель', 'Май', 'Июнь', 'Июль', 'Август', 'Сентябрь', 'Октябрь', 'Ноябрь', 'Декабрь'],
            monthsShort: ['Янв', 'Фев', 'Мар', 'Апр', 'Май', 'Июн', 'Июл', 'Авг', 'Сен', 'Окт', 'Ноя', 'Дек'],
            today: 'Сегодня',
            clear: 'Очистить',
            dateFormat: 'dd/MM/yyyy',
            timeFormat: 'HH:mm',
            firstDay: 1
        },
        autoClose: true,
        startDate: new Date(),
        // selectedDates: [new Date()],
        // inline: true,
        mobile: true,
    })
})

function fix100vh() {
    let vh = window.innerHeight * 0.01;
    document.documentElement.style.setProperty('--vh', `${vh}px`);
}

//# sourceMappingURL=../sourcemaps/main.min.js.map
